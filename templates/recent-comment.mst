{{ #response }}

<li class="disqus-recentcomment">
  {{ #author.name }}
  <span class="disqus-recentcomment-name">{{ author.name }}</span>
  {{ /author.name }}

  {{ ^author.name }}
  <span class="disqus-recentcomment-name">{{ author.username }}</span>
  {{ /author.name }}

  <span class="disqus-recentcomment-date">{{ dateFormatted }}</span>

  <a target="_blank" class="disqus-recentcomment-content" href="{{ url }}">{{ raw_message }}</a>
</li>

{{ /response }}
